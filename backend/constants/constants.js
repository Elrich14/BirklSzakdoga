// ==================== ORDER ====================

const ORDER_STATUSES = [
  "pending",
  "confirmed",
  "shipped",
  "delivered",
  "cancelled",
];

const DEFAULT_ORDER_PAGE_SIZE = 20;

module.exports = {
  ORDER_STATUSES,
  DEFAULT_ORDER_PAGE_SIZE,
};
